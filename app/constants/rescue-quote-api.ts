/** Active: create quote for a rescue. */
export const RESCUE_QUOTE_CREATE_PATH = '/api/rescue/quote/create/';

/** TBD — fetch quote lines for detail tab when backend is ready. */
// export const RESCUE_QUOTE_DETAIL_PATH = (rescueId: number) =>
//   `/api/rescue/quote/${rescueId}/`;

/** TBD — update existing quote when backend is ready. */
// export const RESCUE_QUOTE_UPDATE_PATH = (quoteId: number) =>
//   `/api/rescue/quote/update/${quoteId}/`;
